import React from 'react';
import './btn.scss';

function BtnViolet({title}) {
    return (
        <button className='btn btn-violet' >
            {title}
        </button>
      );
    };

export default BtnViolet;
